// Configuración específica para React Native
import { 
  Platform, 
  Dimensions, 
  StatusBar, 
  Alert, 
  ActivityIndicator, 
  SafeAreaView,
  KeyboardAvoidingView,
  ScrollView,
  Animated,
  RefreshControl
} from 'react-native';

export { 
  Platform, 
  Dimensions, 
  StatusBar, 
  Alert, 
  ActivityIndicator, 
  SafeAreaView,
  KeyboardAvoidingView,
  ScrollView,
  Animated,
  RefreshControl
};
